db:
  environment:
    MYSQL_ROOT_PASSWORD: example
  image: mariadb
  volumes:
  - mysqldata:/var/lib/mysql
typo3:
  environment:
    VIRTUAL_HOST: ${VHOSTNAME}
  ports:
  - 80
  image: np/typo3:0.10
  links:
  - db:mysql
  volumes:
  - typo3data:/var/www/htdocs