db:
  environment:
    MYSQL_ROOT_PASSWORD: example
  image: mariadb
  volumes:
  - ${VHOSTNAME}-mysql:/var/lib/mysql
typo3:
  environment:
    VIRTUAL_HOST: ${VHOSTNAME},www.${VHOSTNAME}
  ports:
  - 80
  image: np/typo3:0.14
  links:
  - db:mysql
  volumes:
  - ${VHOSTNAME}-typo3:/var/www/htdocs